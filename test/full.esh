#| Top comment
   Aren't nested block comments cool?

   P = fun(Str) -> {ok, T, _} = erl_scan:string(Str), {ok, F} = erl_parse:parse_form(T), io:format("~p", [F]) end.
|#

@module#|hey|#syntax.full# [Param, Another,
#AndAnother] # Hope this goes well
@module another.one [Oh, Yeah]
@module another.one [Oh yeah Wowsers]
  # I mean, really really

