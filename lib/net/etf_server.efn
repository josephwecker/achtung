@mod(etf_server) -> (Name, ListenPort)

<start
  Opts =
    port:        ListenPort,
    worker_loop: etf_worker
  gen_server_basic.start(local, Name, socket_server_gs, Opts)

<stop: erlang.send(Name, shutdown)

etf_worker|Sock, Opts|
  gen_server_basic.start_anon(etf_handler_gs, [Sock, Opts])

